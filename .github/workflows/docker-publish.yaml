# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/docker-publish.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
env:
  IMAGE: ghcr.io/${{ github.repository }}
jobs:
  build:
    runs-on: ${{ matrix.platform.runs-on }}
    steps:
    - name: checkout full
      uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
      with:
        fetch-depth: 0
    - name: Install nix
      uses: cachix/install-nix-action@4e002c8ec80594ecd40e759629461e26c8abed15
      with:
        github_access_token: ${{ secrets.GITHUB_TOKEN }}
    - name: Login to GHCR
      uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
      with:
        password: ${{ secrets.GITHUB_TOKEN }}
        registry: ghcr.io
        username: ${{ github.repository_owner }}
    - name: Build and push image
      run: nix run ".#dockerImageFull.copyTo" -- "docker://${{ env.IMAGE }}:${GITHUB_REF_NAME//\//-}-${{
        matrix.platform.target }}"
    strategy:
      matrix:
        platform:
        - os-name: Linux-x86_64
          runs-on: ubuntu-24.04
          target: x86_64-unknown-linux-gnu
        - os-name: Linux-aarch64
          runs-on: ubuntu-24.04-arm
          target: aarch64-unknown-linux-gnu
  manifest:
    needs:
    - build
    runs-on: ubuntu-latest
    steps:
    - name: Login to GHCR
      uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9
      with:
        password: ${{ secrets.GITHUB_TOKEN }}
        registry: ghcr.io
        username: ${{ github.repository_owner }}
    - name: Make manifest
      run: |-
        docker manifest create "${{ env.IMAGE }}:${GITHUB_REF_NAME//\//-}" ${{ env.IMAGE }}:${GITHUB_REF_NAME//\//-}-x86_64-unknown-linux-gnu ${{ env.IMAGE }}:${GITHUB_REF_NAME//\//-}-aarch64-unknown-linux-gnu
        docker manifest push "${{ env.IMAGE }}:${GITHUB_REF_NAME//\//-}"
name: Publish docker image
'on':
  push: {}
