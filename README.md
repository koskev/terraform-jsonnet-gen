# Generate OpenTofu/Terraform bindings for Jsonnet

This small program generates Jsonnet bindings from the used providers. In combination with a language server this allows to autocomplete all the possible options and directly display the relevant documentation on each function.

For example this makes it very easy to define tf resources in the same file as ArgoCD resources.
